export interface ComponentsProps {
    className?: string;
    id?: string;
    onClick? : any;
    children? : React.ReactNode;
    style?: React.CSSProperties;
    width? : string;
    height? : string;
}